#include <stdio.h>

int main() {
    int number = 42;  // Declare and initialize an integer variable
    printf("Hello, World!\n");
    printf("The value of the variable is: %d\n", number);  // Print the value of the variable
    return 0;
}